<body style="cursor: crosshair; outline: rgb(0, 0, 0) dotted 3px; outline-offset: 1px;">


<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Manutenção de Faróis - Comparativo Avançado</title>
<style>
  /* Cores base */
  :root {
    --primary-color: #004080;
    --secondary-color: #007700;
    --background-color: #f2f2f2;
    --text-color: #333333;
    --highlight-color: #009933;
    --slider-color: #007700;
  }

  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: var(--background-color);
    margin: 0;
    padding: 0;
    color: var(--text-color);
  }

  header {
    background-color: var(--primary-color);
    color: white;
    padding: 25px 20px;
    text-align: center;
    box-shadow: 0 3px 8px rgba(0,0,0,0.3);
  }

  main {
    max-width: 960px;
    margin: 40px auto 80px;
    background: white;
    padding: 40px 35px;
    border-radius: 12px;
    box-shadow: 0 6px 20px rgba(0,0,0,0.1);
  }

  h1, h2 {
    margin-top: 0;
    color: var(--primary-color);
  }

  p {
    line-height: 1.6;
    font-size: 1.1em;
  }

  .price {
    font-size: 1.8em;
    color: var(--secondary-color);
    font-weight: 700;
    margin: 25px 0 40px;
  }

  /* Seção comparativa avançada */
  .comparison-wrapper {
    margin-bottom: 50px;
  }

  .comparison-title {
    font-weight: 700;
    font-size: 1.6em;
    margin-bottom: 15px;
    border-bottom: 3px solid var(--highlight-color);
    padding-bottom: 8px;
  }

  .comparison-container {
    position: relative;
    width: 100%;
    max-width: 100%;
    height: 360px;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(0,0,0,0.15);
    user-select: none;
  }

  .comparison-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .img-after {
    position: relative;
    z-index: 1;
  }

  .img-before {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 50%;
    overflow: hidden;
    z-index: 2;
    border-radius: 12px 0 0 12px;
    box-shadow: inset -5px 0 8px -4px rgba(0,0,0,0.3);
  }

  .img-before img {
    position: relative;
    left: 0;
    transition: left 0.3s ease;
  }

  .slider {
    position: absolute;
    top: 0;
    left: 50%;
    width: 5px;
    height: 100%;
    background: var(--slider-color);
    cursor: ew-resize;
    z-index: 3;
    border-radius: 3px;
    box-shadow: 0 0 8px var(--slider-color);
    transition: background 0.3s ease;
  }

  .slider:hover {
    background: var(--highlight-color);
    box-shadow: 0 0 12px var(--highlight-color);
  }

  .labels {
    display: flex;
    justify-content: space-between;
    margin-top: 12px;
    font-weight: 700;
    color: var(--primary-color);
    font-size: 1.1em;
  }

  /* Seção de múltiplos comparativos */
  .multi-comparison {
    display: grid;
    grid-template-columns: repeat(auto-fit,minmax(280px,1fr));
    gap: 30px;
  }

  .comparison-card {
    background: #fafafa;
    border-radius: 10px;
    box-shadow: 0 3px 12px rgba(0,0,0,0.1);
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  .comparison-card img {
    height: 180px;
    object-fit: cover;
    width: 100%;
  }

  .comparison-card-content {
    padding: 15px 20px;
    flex-grow: 1;
  }

  .comparison-card-content h3 {
    margin: 0 0 10px;
    color: var(--secondary-color);
  }

  .comparison-card-content p {
    font-size: 0.95em;
    color: #555;
  }

  /* Contato */
  .contact {
    margin-top: 50px;
    border-top: 2px solid var(--primary-color);
    padding-top: 30px;
  }

  .contact h2 {
    margin-bottom: 15px;
  }

  .contact p {
    font-size: 1.1em;
    margin: 8px 0;
  }

  /* Rodapé fixo */
  footer {
    text-align: center;
    padding: 18px 10px;
    background: var(--primary-color);
    color: white;
    position: fixed;
    width: 100%;
    bottom: 0;
    font-size: 0.9em;
    box-shadow: 0 -3px 8px rgba(0,0,0,0.3);
  }

  /* Responsividade */
  @media (max-width: 700px) {
    .comparison-container {
      height: 280px;
    }
    .comparison-card img {
      height: 140px;
    }
  }
</style>


<header style="">
  <h1 style="">Manutenção de Faróis de Carros</h1>
</header>
<main>
  <p>Serviço profissional de restauração e manutenção de faróis para melhorar a visibilidade e estética do seu veículo.</p>
  <p class="price">Preço: 25 euros por farol</p>

  <section class="comparison-wrapper">
    <h2 class="comparison-title">Comparativo Avançado: Antes e Depois da Manutenção</h2>
    <div class="comparison-container" id="comparison">
      <div class="img-after">
        <img src="https://i.imgur.com/7Q9QXqv.jpg" alt="Farol Depois da Manutenção">
      </div>
      <div class="img-before" id="img-before" style="width: 382.8px;">
        <img src="https://i.imgur.com/0Xq6Xqv.jpg" alt="Farol Antes da Manutenção">
      </div>
      <div class="slider" id="slider" aria-label="Controle deslizante para comparar imagens antes e depois" role="slider" tabindex="0" aria-valuemin="0" aria-valuemax="100" aria-valuenow="46" style="left: 382.8px;"></div>
    </div>
    <div class="labels">
      <span>Antes</span>
      <span>Depois</span>
    </div>
  </section>

  <section>
    <h2 class="comparison-title">Outros Casos de Sucesso</h2>
    <div class="multi-comparison">
      <div class="comparison-card">
        <img src="https://i.imgur.com/1Xq6Xqv.jpg" alt="Farol amarelado antes">
        <img src="https://i.imgur.com/2Q9QXqv.jpg" alt="Farol restaurado depois" style="display:none;">
        <div class="comparison-card-content">
          <h3>Farol Amarelado</h3>
          <p>Restauração completa que removeu o amarelado e recuperou a transparência original, melhorando a segurança noturna.</p>
        </div>
      </div>
      <div class="comparison-card">
        <img src="https://i.imgur.com/3Xq6Xqv.jpg" alt="Farol riscado antes">
        <img src="https://i.imgur.com/4Q9QXqv.jpg" alt="Farol polido depois" style="display:none;">
        <div class="comparison-card-content">
          <h3>Farol Riscado</h3>
          <p>Polimento profissional que eliminou riscos profundos, restaurando o brilho e a eficiência da iluminação.</p>
        </div>
      </div>
      <div class="comparison-card">
        <img src="https://i.imgur.com/5Xq6Xqv.jpg" alt="Farol opaco antes">
        <img src="https://i.imgur.com/6Q9QXqv.jpg" alt="Farol limpo depois" style="display:none;">
        <div class="comparison-card-content">
          <h3>Farol Opaco</h3>
          <p>Limpeza e tratamento que devolveram a clareza, aumentando a visibilidade e o valor estético do veículo.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="contact">
    <h2>Contato</h2>
    <p>Email: contato@manutencaofarois.com</p>
    <p>Telefone: +55 11 99999-9999</p>
  </section>
</main>
<footer>
  © 2025 Manutenção de Faróis - Todos os direitos reservados
</footer>

<script>
  // Slider interativo para comparação antes e depois
  const comparison = document.getElementById('comparison');
  const imgBefore = document.getElementById('img-before');
  const slider = document.getElementById('slider');
  let isDragging = false;

  function setPosition(x) {
    const rect = comparison.getBoundingClientRect();
    let position = x - rect.left;
    if (position < 0) position = 0;
    if (position > rect.width) position = rect.width;
    imgBefore.style.width = position + 'px';
    slider.style.left = position + 'px';

    // Atualiza atributo aria-valuenow para acessibilidade
    let percent = Math.round((position / rect.width) * 100);
    slider.setAttribute('aria-valuenow', percent);
  }

  slider.addEventListener('mousedown', () => {
    isDragging = true;
  });

  window.addEventListener('mouseup', () => {
    isDragging = false;
  });

  window.addEventListener('mousemove', (e) => {
    if (!isDragging) return;
    setPosition(e.clientX);
  });

  // Toque para dispositivos móveis
  slider.addEventListener('touchstart', () => {
    isDragging = true;
  });

  window.addEventListener('touchend', () => {
    isDragging = false;
  });

  window.addEventListener('touchmove', (e) => {
    if (!isDragging) return;
    setPosition(e.touches[0].clientX);
  });

  // Inicializa slider no meio
  window.addEventListener('load', () => {
    const rect = comparison.getBoundingClientRect();
    setPosition(rect.width / 2);
  });

  // Acessibilidade: controle via teclado
  slider.addEventListener('keydown', (e) => {
    const rect = comparison.getBoundingClientRect();
    let currentWidth = imgBefore.getBoundingClientRect().width;
    if (e.key === 'ArrowLeft' || e.key === 'ArrowDown') {
      e.preventDefault();
      let newPos = currentWidth - 15;
      if (newPos < 0) newPos = 0;
      setPosition(rect.left + newPos);
    } else if (e.key === 'ArrowRight' || e.key === 'ArrowUp') {
      e.preventDefault();
      let newPos = currentWidth + 15;
      if (newPos > rect.width) newPos = rect.width;
      setPosition(rect.left + newPos);
    }
  });
</script>



        
      </body>
